<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        <header>
            <nav aria-label="Navigation principale">
                <a href="/">Accueil</a>

                <span style="margin-left: 1rem;">Catégories :</span>
                <ul style="display:inline-flex; gap:.75rem; list-style:none; padding:0; margin:.5rem 0 0 0;">
                    {% for categorie in categories|default([]) %}
                        <li>
                            {# Évite path() tant que la route Symfony n'existe pas #}
                            <a href="#"
                               class="js-categorie"
                               data-id="{{ categorie.id }}">
                                {{ categorie.nom }}
                            </a>
                        </li>
                    {% endfor %}
                </ul>
            </nav>
        </header>

        {% block body %}{% endblock %}
    </body>
</html>